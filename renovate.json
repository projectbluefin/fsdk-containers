{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":dependencyDashboard",
    "helpers:pinGitHubActionDigests"
  ],
  "enabledManagers": [
    "github-actions",
    "custom.regex"
  ],
  "schedule": [
    "after 2am and before 6am"
  ],
  "customManagers": [
    {
      "description": "BuildStream elements: any value annotated with a '# renovate:' comment on the preceding line. Covers `<tool>_version:` variables, `track:` pins and pinned tarball URLs.",
      "customType": "regex",
      "managerFilePatterns": [
        "/\\.bst$/",
        "/^Justfile$/"
      ],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>\\S+) depName=(?<depName>\\S+)(?: versioning=(?<versioning>\\S+))?(?: extractVersion=(?<extractVersion>\\S+))?\\s+[^\\n]*?[:=]\\s*\"?(?<currentValue>[\\w][\\w.+-]*)\"?"
      ],
      "versioningTemplate": "{{#if versioning}}{{{versioning}}}{{else}}semver{{/if}}"
    },
    {
      "description": "Pinned Python dependencies for the catalog / verify workflows (.github/requirements/verify.txt). Matches `dep==version` lines (hashes follow on the same line) so pinned PyPI packages stay current under the human-review gate below.",
      "customType": "regex",
      "managerFilePatterns": [
        "/\\.github/requirements/.*\\.txt$/"
      ],
      "matchStrings": [
        "(?<depName>[A-Za-z0-9][A-Za-z0-9._-]*)==(?<currentValue>[0-9][A-Za-z0-9.+_-]*)"
      ],
      "datasourceTemplate": "pypi",
      "versioningTemplate": "pep440"
    }
  ],
  "packageRules": [
    {
      "matchManagers": [
        "github-actions"
      ],
      "groupName": "github-actions-non-major",
      "matchUpdateTypes": [
        "minor",
        "patch",
        "digest",
        "pin",
        "pinDigest"
      ],
      "description": "Never automerge GitHub Actions bumps: digest pinning blunts tag-rewriting but not a compromised release under a new SHA. Workflows here handle mergeraptor tokens, cosign signing, and packages:write, so every action update gets human review (mirrors the custom.regex gate below).",
      "automerge": false
    },
    {
      "matchManagers": [
        "github-actions"
      ],
      "matchUpdateTypes": [
        "major"
      ],
      "automerge": false
    },
    {
      "description": "Upstream software pinned in BuildStream elements. Never automerge: the sha256/commit ref must be refreshed by `bst source track` (see .github/workflows/refresh-bst-refs.yml) and the image must pass `just verify` first.",
      "matchManagers": [
        "custom.regex"
      ],
      "matchFileNames": [
        "**/*.bst",
        "Justfile"
      ],
      "automerge": false,
      "labels": [
        "dependencies",
        "buildstream"
      ]
    },
    {
      "description": "Pinned PyPI packages in .github/requirements/*.txt. Never automerge: this manager rewrites only the `==` version, so the committed sha256 hashes go stale and `pip install --require-hashes --only-binary=:all:` rejects the bump. Regenerate the hashes before merging -- see docs/skills/ci-tooling/SKILL.md, 'Regenerating .github/requirements hashes'. `bst source track` is NOT involved.",
      "matchManagers": [
        "custom.regex"
      ],
      "matchDatasources": [
        "pypi"
      ],
      "automerge": false,
      "labels": [
        "dependencies",
        "python"
      ]
    },
    {
      "description": "kubectl follows the Kubernetes release cadence; skip pre-releases.",
      "matchDepNames": [
        "kubernetes/kubernetes"
      ],
      "ignoreUnstable": true
    }
  ]
}
